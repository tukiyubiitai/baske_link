name: basketball_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.3 <3.0.0'


dependencies:
  flutter:
    sdk: flutter



  cupertino_icons:
  google_maps_flutter:
  geolocator:
  adaptive_dialog:
  http:
  http_parser:
  path_provider:
  path:
  geocoding:
  image_picker:
  google_sign_in:
  font_awesome_flutter:
  flutter_datetime_picker:
  intl:
  google_place:

  firebase_database:
  firebase_storage:
  firebase_core:
  cloud_firestore:
  firebase_auth:
  cloud_functions: ^4.5.8
  sign_in_with_apple:
  flutter_signin_button:
  dropdown_button2:
  image_cropper:
  flutter_rating_bar:
  url_launcher:
  flutter_slidable:
  share_plus:
  firebase_messaging:
  flutter_local_notifications:
  flutter_email_sender: ^6.0.2
  dots_indicator: ^3.0.0
  flutter_riverpod:
  riverpod_annotation:
  freezed_annotation:
  expand_widget: ^3.1.1
  shared_preferences: ^2.2.2
  logger: ^2.0.2+1



#  flutter_lints:







dev_dependencies:
  flutter_test:
    sdk: flutter

  json_serializable:
  flutter_launcher_icons:
  freezed:
  build_runner:
  riverpod_generator:

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/basketball_icon.png"




flutter:

  uses-material-design: true

  assets:
    - assets/images/

